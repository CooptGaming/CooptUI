; Keep - Item Types (never sold)
; Format: Types separated by /

[Items]
; Augments are progression items — kept by default so Auto Sell never vendors them.
; Per-item exceptions go on the Augment Always Sell list (right-click an augment).
types=Augmentation
