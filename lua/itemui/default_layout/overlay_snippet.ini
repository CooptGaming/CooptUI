; CoOpt UI ImGui overlay snippet (window positions/sizes, dock).
; Captured by coopt_layout_capture.py; empty if no overlay data was captured.
; On first-run or Revert, this is merged into config/MacroQuest_Overlay.ini.
